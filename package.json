{
  "name": "management-tools-workspace",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "lint": "bun run --filter management-tools-cli lint",
    "format": "bun run --filter management-tools-cli format",
    "typecheck": "bun run --filter management-tools-cli typecheck",
    "test": "bun run --filter management-tools-cli test",
    "build:bundle": "bun run --filter management-tools-cli build:bundle",
    "build:binary": "bun run --filter management-tools-cli build:binary",
    "check_all": "bun run --filter management-tools-cli check_all",
    "check-and-build": "bun run --filter management-tools-cli check-and-build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
